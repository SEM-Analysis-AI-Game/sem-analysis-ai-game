/**
 * @param correct The 
 */
export const scoringFunction = (resolution: [number, number], correct: number[], given: number[]) => {
    const [width, height] = resolution; 

    // compare correct vs given and return a number from 0 to 1

    return 0;
}